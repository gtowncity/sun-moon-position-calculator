# Validation Scripts

This folder is reserved for scripts or saved query recipes that generate or refresh validation fixtures.

Do not commit invented reference values. Replace `TODO_REFERENCE_VALUE` only with values obtained from JPL Horizons, NREL SPA-compatible reference material, or another documented primary source.
